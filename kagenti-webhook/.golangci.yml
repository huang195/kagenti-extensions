linters:
  enable:
    - errcheck
    - gosec
    - govet
    - staticcheck
    - unused
    - gofmt
    - ineffassign
    - typecheck

run:
  timeout: 5m

issues:
  exclude-dirs:
    - bin
    - config
    - hack
    - dist
